<li>
  <h2><%= theme %></h2>
  <ul>
    <% theme.discourses.each do |discourse| %>
      <li>
        <%= link_to discourse.title, conference_theme_project_discourse_path(@current_conference, theme, discourse.project, discourse) %>
        <br />
        Средняя оценка: <%= discourse.average_rate %>
        <br />
        Кол-во экспертов: <%= discourse.experts.count %> / <%= theme.experts.count %>
        <br />

        <% late = (theme.experts.map(&:to_s) - discourse.experts.map(&:to_s)) %>
        <% if late.any? %>
          <span class="warning">
            Следующие эксперты, еще не оценили доклад:
            <%= late.to_sentence %>
          </span>
        <% end %>
      </li>
    <% end %>
  </ul>
</li>
